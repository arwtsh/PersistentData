//Somewhere in code, initialize a singleton like this:

/*
 * var singleton = new SavableData<Settings>(new PlayerPrefSaver("settings"));
 */

// After that, you can get the settings with the Get() method

/*
 * singleton.Get().testSetting = 5;
 */
